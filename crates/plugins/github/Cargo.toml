[package]
name = "github"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
quickchat_plugin_sdk = { version = "2.0.0", path = "../../quickchat_plugin_sdk" }
quickchat_types = { version = "2.0.0", path = "../../quickchat_types" }
