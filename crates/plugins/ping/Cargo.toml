[package]
name = "ping_plugin"
version = "2.0.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
quickchat_plugin_sdk = { path = "../../quickchat_plugin_sdk" }
quickchat_types = { path = "../../quickchat_types" }
