[package]
name = "quickchat_dht"
version = "0.1.0"
edition = "2024"

[dependencies]
libp2p = { version = "0.56.0", default-features = false, features = ["tokio", "kad", "noise", "yamux", "tcp", "macros", "relay"] }
tokio = { version = "1.53.1", features = ["full"] }
