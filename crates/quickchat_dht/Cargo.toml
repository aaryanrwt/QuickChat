[package]
name = "quickchat_dht"
version = "0.1.0"
edition = "2024"

[dependencies]
libp2p = { version = "0.56.0", features = ["tokio", "kad", "noise", "yamux", "tcp", "macros", "mdns"] }
tokio = { version = "1.53.1", features = ["full"] }
